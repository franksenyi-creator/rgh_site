#!/data/data/com.termux/files/usr/bin/bash

cd ~/rgh-site

cp index.html index.preflight.html

git add index.html
git commit -m "Pre-flight checkpoint" || echo "Nothing to commit, already clean."

git push#!/data/data/com.termux/files/usr/bin/bash

cd ~/rgh-site

cp index.html index.preflight.html

git add index.html
git commit -m "Pre-flight checkpoint" || echo "Nothing to commit, already clean."

git push
#!/data/data/com.termux/files/usr/bin/bash

cd ~/rgh-site

cp index.html index.preflight.html

git add index.html
git commit -m "Pre-flight checkpoint" || echo "Nothing to commit, already clean."

git push
